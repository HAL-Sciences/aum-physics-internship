fun_numbers = [3.14, 2.72, 9.8, 6.18, 1.414]
print('The first number in the list is pi, which is about ', fun_numbers[0]) # prints 3.14
print('The middlemost number in the list is the acceleration due to gravity, which is roughly ' + str(fun_numbers[2])) # prints 9.8
print('The final number in the list is the square root of 2, which is approximately', fun_numbers[-1]) # prints 1.414